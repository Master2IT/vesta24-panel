const app = {
  state: {
    // loading: false,
  },
  mutations: {
    // DISABLE: (state, disable) => {
    //   state.disable = disable;
    // },
  },
  actions: {
    // disable({
    //   commit
    // }, disable) {
    //   commit('DISABLE', disable);
    // },
  }
};

export default app